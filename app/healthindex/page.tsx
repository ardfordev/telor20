import React, { FC } from 'react'
import HealthIndexContent from './healthindex-content'

interface pageProps {
  
}

const page: FC<pageProps> = ({  }) => {
  return (
    <>
      <HealthIndexContent />
    </>
  )
}

export default page;